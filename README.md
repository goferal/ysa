# Your Style Archetype

Static site: Next.js (static export) + Tailwind + markdown content. See `CLAUDE.md` for how everything is organized and how to add posts, guides, and change prices.

```bash
npm install
npm run dev     # local preview at http://localhost:3000
npm run build   # static site in ./out
```
